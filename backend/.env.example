NODE_ENV=development
PORT=3000

# --- DATABASE (DOCKER) ---
# Adjust DB_PORT to 5433 if running alongside Docker, or 5432 if standalone.
DB_USERNAME=postgres
DB_PASSWORD=postgrespassword
DB_NAME=inventory_db
DB_HOST=127.0.0.1
DB_DIALECT=postgres
DB_PORT=5433

# --- VENDOR INTEGRATION ---
VENDOR_API_URL=replace_with_vendor_api_url
VENDOR_SECRET_KEY=replace_with_your_secret_key